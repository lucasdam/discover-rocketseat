<!DOCTYPE html>

<%- include('../partials/head'); %> <!-- Falando que será dado um comando EJS. No caso, um comando de incluir (import) -->

<html lang="pt-br">

<body class="container">

    <%- include('../partials/header'); %>

    <main>
        <div class="jumbotron">
            <h1>Node com EJS</h1>
            <p>Vamos aprender a trabalhar com arquivos EJS</p>
            <p><%= subtitle %></p>
        </div>

        <ul>
            <% qualitys.forEach(function(quality) { %> <!-- Função anônima -->
                <li>
                    <strong><%= quality.title %></strong> <%= quality.message %>
                </li>
            <% }) %>
        </ul>

    </main>

    <%- include('../partials/footer'); %>

</body>

</html>